import axios from 'axios';

const API_URL = import.meta.env.VITE_API_URL;

// Placeholder for future user-related API calls if needed
// Currently empty as user data is extracted from JWT token
export const placeholder = () => {};